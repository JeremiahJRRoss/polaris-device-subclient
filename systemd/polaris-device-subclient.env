# Polaris Device Subclient — Environment Variables
# This file is loaded by systemd (EnvironmentFile=).
# Permissions should be 0600 root:root.

# Required: Polaris API key (Personal Access Token)
POLARIS_API_KEY=CHANGE_ME

# Required: Polaris GraphQL subscription endpoint
POLARIS_API_URL=wss://graphql.pointonenav.com/subscriptions

# Optional: Log level (debug, info, warn, error)
# POLARIS_LOG_LEVEL=info

# Optional: Output mode override (stdout or file)
# POLARIS_OUTPUT=file

# Optional: Output directory override
# POLARIS_OUTPUT_DIR=/var/lib/polaris/data

# Optional: Config file path override
# POLARIS_CONFIG=/etc/polaris/config.json

# Optional: Encrypted secrets key file
# POLARIS_KEY_FILE=/etc/polaris/master.key
